# BOOKFIGHT — 책들의 입씨름 단판승부 설계 노트

책 두 권을 옥타곤에 올려 **발췌문으로 입씨름**을 시키고, 그 과정을 **UFC 중계처럼** 보여주는 게임.
빌드 과정 없이 정적 파일만으로 돌아간다(three.js r166 벤더링, 외부 요청 0건).

## 0. 45초 3막 — 길이와 리듬

경기가 길면 지루하고, 카메라도 같은 컷을 반복하게 된다. 그래서 **45초로 끊고 3막으로 짰다.**
경기 시계 = 재생 시간(1:1)이라 시계가 0이 되는 순간 실제로 종이 울린다.

    0~4.5s   오프닝 러시 (펀치)
    ~9s      1합 — 탐색            [어깨너머 컷]
    ~17s     펀치
    ~22s     2합 — 되받기          [타이트 싱글]  ← 1합을 접속구로 문다
    ~31s     펀치
    ~33s     '마지막 공방' 아크     [눈높이 반원]
    ~38s     3합 — 결정타          [슬로 푸시]    ← 처음 들었던 말을 콜백
    ~45s     잔여 난타 → 종

다운 2회면 그대로 TKO — 45초에 3다운까지 갈 시간이 없다.
실측: KO 81% / 판정 19%, 평균 45.8초(최대 51.0), 승률 스프레드 24pt, 시드 뒤집힘 99%.

## 0-1. 설전이 앞 대사를 문다 — 스레드

합이 3개뿐이라 **하나의 논쟁으로 이을 수 있다.** 아무말 대잔치를 막는 장치는 세 개다.

1. **주제 고정** — 도발과 반박은 같은 태그를 놓고 붙는다(검증 100% 일치)
2. **접속구** — 2합은 1합의 결과를 물고 시작한다
   - 1합을 논파당했으면 `comeback`: 「그 말, 그대로 돌려드리지. …」
   - 1합을 논파했으면 `press`: 「말문이 막히신 김에 하나 더. …」
3. **콜백** — 3합은 **자기가 들었던 공격을 인용**해 끝낸다
   - 「나더러 **어렵다** 하셨소. 그 말은 마지막 문장으로 갚겠소.」

실제로 이렇게 이어진다:

    ▶ [드라큘라]   r1/open     #얄팍함  「96쪽이라. 나는 그보다 오래 관 속에 누워 있었소.」
    ↩ [앨리스]     논파                「한 문장으로 사람을 바꿀 수 있다면 한 문장이면 되오.」
    ▶ [앨리스]     r2/press    #허황    「말문이 막히신 김에 하나 더. 당신은 세상을 그린 게 아니라 꿈을 적었소.」
    ↩ [드라큘라]   못 막음             「당신은 내가 없다고 믿소. 좋소. 그게 내가 가장 좋아하는 조건이오.」
    ▶ [앨리스]     r3/callback #얄팍함  「얇다고 하셨나. 그 한마디로 당신의 결말이 정해졌소.」
    ↩ [드라큘라]   항변                「아직… 아직이오.」

결정타는 어지간해선 안 뒤집힌다(논파 확률 절반). 뒤집히면 그게 **대반전**이고, 약자의 반격이 1.35배로 들어간다.

## 0-2. 두 층으로 나뉜다 — 리듬의 문제

권투가 대사를 기다리면 리듬이 죽는다. 그래서 두 층을 따로 돌린다.

    권투층 boxing.js  잽·스트레이트·훅·어퍼·보디가 0.5~0.8초 간격으로 쉬지 않고 오간다
    설전층 taunts.js  콤비네이션 사이에 끼어들어 치명타를 만든다

    🥊 잽 -3   🥊 잽 가드   🥊 보디 -3   🥊 보디 -3
    💬 [햄릿] #얄팍함 → 💬 [자기만의 방] 못 막음 → 💥 정타 -23 CRIT
    🥊 잽 가드   🥊 훅 헛침
    💬 [햄릿] #얄팍함 → …

`boxing.js`는 books.js와 rng.js만 안다. 대사·태그·논파를 전혀 모르므로 **단독으로 시뮬레이션하고
검증할 수 있다.** 한 경기는 펀치 28방 + 설전 7.8합 · 약 85초, 권투가 전체 피해의 41%를 담당한다.

## 0-3. 검증 도구

두 층을 각각 스크립트 레벨에서만 검증한다(브라우저·3D 없이).

```bash
node bookfight/tools/verify-dialogue.mjs   # 대화·논파
node bookfight/tools/verify-boxing.mjs     # 권투 시뮬레이션 (대사 0줄)
```

- **verify-dialogue** — 도발/반박의 주제 일치, 미치환 슬롯, 한국어 조사, 대사 중복,
  논파율 분포, 16권·10태그 커버리지, 그리고 **전용 대사가 0줄인 낯선 책도 성립하는지**
- **verify-boxing** — 펀치 분포, 명중/가드/회피 비율, 한 방 피해가 체력에서 차지하는 비중,
  스태미나 곡선, 주먹 간격, 그리고 **능력치가 실제로 작동하는지**(광기↔회피, 관록↔가드, 기동↔선공을 상관계수로)

이 도구들이 실제로 잡아낸 것: 보디블로가 한 번도 안 나오던 문제, 회피율 40%,
관록이 86~100에만 몰려 가드가 사실상 고정값이던 문제, 검증 스크립트 자체의 단위 혼용 버그.

## 1. 설전 시스템 — 태그와 2층 구조

명대사를 번갈아 읊으면 그건 언쟁이 아니라 **두 개의 독백**이다. 그래서 한 합을
**"도발 → 반박 → 판정 → 주먹"**으로 짰다. 주먹이 누구에게 들어갈지는 주사위가 아니라
**말싸움 결과**가 정한다. 논파하면 공수가 뒤집혀 반격이 들어간다.

공격은 **상대 책의 약점 태그를 찌르는 것**이고 방어는 **그 태그를 변호하는 것**이다.
둘이 같은 주제를 놓고 말하므로 앞뒤가 맞는다.

태그 10종: `#장황함 #얄팍함 #난해함 #유치함 #낡음 #통속 #설교조 #편협 #허황 #음울`

> **앨리스** → 모비 딕의 `#장황함`
> 「635쪽이요? 처음부터 시작해서 끝까지 가고, 거기서 멈추면 됐잖아요.」
> **모비 딕** (되받지 못함)
> 「이 배는 목적지로 가는 배가 아니오. 항해 자체가 이야기요.」

### 2층 구조 — 어떤 책이든 싸울 수 있어야 한다

손으로 16권 × 8줄을 쓰면 그 16권 밖으로 못 나간다. 그래서:

- **1층 `taunts.js`** — 쪽수·출간연도·장르에서 태그를 자동으로 뽑고, 태그별 템플릿에 상대 책의
  실제 수치를 꽂는다. **전용 대사가 한 줄도 없어도 성립한다.** 검색으로 긁어온 낯선 책도 링에 오른다.
- **2층 `voices.js`** — 큐레이션된 16권의 전용 도발/방어/상성 저격. 있으면 우선.

**중요:** 전용 대사 보유 여부가 승률을 좌우하면 안 된다. 그러면 손으로 쓴 대사가 많은 책이 그냥 세지고
API로 가져온 책은 이길 방법이 없어진다. 그래서 전용 대사는 논파 확률에 +0.06만 더하고,
실제 판정은 능력치로 가른다.

### 찌르는 힘 ≠ 받아치는 힘

    설득력(공격) = 논지 0.65 + 문체 0.35   → 피해량도 논지가 정한다
    반박력(방어) = 뚝심 0.45 + 광기 0.3 + 관록 0.25

논지가 양쪽을 다 결정하게 두면 논지 높은 책이 피해량과 논파율을 동시에 챙겨 승률이 폭주한다
(실측 스프레드 44pt). 갈라놓으니 22pt로 내려왔다.

## 2. 핵심 규칙 — 실제 책의 속성이 곧 능력치

| 실제 속성 | 게임 안에서 |
|---|---|
| 분량(쪽) | 체력 · 스태미나 총량 · 기동력(선공률) · 체급 · 3D 두께 |
| 출간연도 | 관록 보너스(오래 버틴 책일수록 피해 감소) |
| 저자의 성격 | 필살기(저자 특성) 1종 |
| 대표 문장 | 실제 타격. 잽/훅/피니시가 전부 그 책의 발췌문이다 |
| 표지 | 3D 모델 그 자체(앞표지=입이자 주먹) |

능력치 5종은 `logic 논지 / style 문체 / legacy 관록 / chaos 광기 / grit 뚝심`.
`books.js`의 `stats`는 **작품의 성격**만 적어두고, 밸런스 보정은 전부 `deriveStats()`가 한다.

### 체급 균형이 잡히는 지점

두꺼운 책은 체력이 많고, 얇은 책은 선공을 더 자주 잡는다. 한 라운드에서 A가 이기려면

```
선공비 pA/(1-pA)  >  HP_B / HP_A
```

가장 극단인 74쪽(변신) 대 1225쪽(전쟁과 평화)에서 두 값이 엇비슷해지도록
`TUNING.INITIATIVE_DIV`를 잡았다. 여기에 더해 `BALANCE.SUM_SLOPE`가 분량에 비례해 능력치
총합을 깎아, **두껍고 스탯도 높은 이중 우위**를 없앤다.

### 실측 밸런스 (16권 × 120대진 × 시드 24개, `sweep2.mjs`)

- KO/TKO 81% · 판정 19% — 스코어카드가 실제로 보이는 비율
- 책별 승률 스프레드 24pt
- 한 경기 = 45초 (평균 45.8초, 최대 51.0)
- **시드에 따라 승패가 갈리는 대진 120/120 (100%)**

## 2. 시드 — 단판인데도 매번 다른 이유

단판 1라운드라 운이 크게 작용한다. 그 운을 전부 시드 하나에 묶었다.

- `Rng`(xmur3 해시 + mulberry32)만 사용. 시뮬레이션에서 `Math.random()` 금지.
- 시드는 **컨디션**(±10% 논지 보정)까지 정한다 — 계체량 화면에 미리 표시된다.
- 같은 시드 + 같은 대진 = 언제나 같은 경기. `?seed=100001&red=mobydick&blue=alice`로 공유 가능.
- 연속 시드(100001, 100002…)도 첫 난수 분포가 균일함을 확인했다(10000개 표본, 편차 ±5%).

연출용 난수는 `Director.rand()`로 완전히 분리했다. **카메라가 승패에 영향을 주지 않는다.**

## 3. 구조 — 시뮬레이션과 연출의 분리

```
match.js  시드 → 경기 전체를 이벤트 타임라인으로 한 번에 계산
             ↓  (events[] : strike / knockdown / replay / finish / decision …)
main.js   타임라인을 "재생"만 한다. 승패에 관여하는 판단을 절대 하지 않는다
             ↓
fighter.js  3D 책 모션      director.js  카메라 컷      hud.js  방송 그래픽
```

이 분리 덕분에 리플레이·되감기·건너뛰기·시드 공유가 전부 공짜로 따라온다.

### 시계가 두 개인 이유

`real`(벽시계)로 타임라인·카메라·관중을 돌리고, `show`(= real × 슬로모 배속)로 파이터 모션을 돌린다.
하나로 묶고 dt를 0.05로 캡하면 **20fps 아래에서 경기 전체가 슬로모로 늘어진다.**
프레임만 떨어져야지 중계 속도가 느려지면 안 된다.

## 4. 3D 책 파이터 — 가분수 포테이토헤드 복서

책이 머리이자 몸통이고, 짧은 팔다리·글러브·신발·머리카락이 거기 **꽂힌다**(`rig.js`).

**얼굴은 표지에 인쇄된 스티커다.** 처음엔 구체 눈알을 얹었는데 책 위에 떠 있는 별개 부속처럼
보였다. 평면 원판(조명을 무시하는 `MeshBasicMaterial`)으로 바꾸니 어느 조명에서도 또렷한
만화 얼굴이 됐다. **표정은 눈썹 기울기가 전부** — 공격 중엔 찌푸리고, 아프면 처지고,
KO엔 X자 눈이 뜬다. 입은 대사 리듬으로 벌어지고, 표지 플랩은 피니시·KO 전용으로 아꼈다.

팔다리는 살구색이 뼈다귀처럼 보여 **짙은 고무호스 색**으로 바꿨고, 머리카락은 책이 얇은 판이라
**앞뒤로 0.4배 눌러야** 우산이 아니라 머리로 읽힌다.

- **머리 모양 16종**이 실루엣만으로 누구인지 알려준다 — 햄릿 왕관, 프랑켄슈타인 각진 번개머리,
  드라큘라 뾰족 올백, 다윈 대머리+수염, 앨리스 리본 금발, 돈키호테 놋대야 투구,
  지킬은 **반반 머리**(한쪽 단정, 한쪽 헝클어짐)
- 고무호스 사지 FK — 가드/잽/훅(뒷손)/어퍼, 맞으면 가드가 풀리고 고개가 젖혀진다,
  다운되면 다리를 뻗고 널브러진다
- 앞표지가 **책등을 경첩 삼아** 대사 리듬으로 여닫힌다 = 말하는 입
- 타격이 꽂히면 **발췌문이 인쇄된 종이**(`QuoteSlip`)가 실제로 날아가 상대를 때린다.
- 맞으면 낱장이 터져 나오고(파티클 34장), 다운되면 펼쳐진 채 매트에 엎어진다.
- 앞표지/뒤표지/책등 전부 캔버스로 그린다(`cover.js`). 뒤표지에는 소개글·분류·바코드까지 —
  오빗 컷에서 화면을 가득 채우는 면이라 비워둘 수 없다.
- 책등 텍스처는 좌우를 미리 뒤집는다. BoxGeometry의 -X 면 UV가 +X와 반대라 그냥 두면 거울 글씨가 된다.

## 5. 카메라 — 12종 컷

`dialogue · dlgTight · dlgPush · arc · impact · insert · wide · lowAngle · overhead · closeUp · ots · dutch · cageCam · skyCam · corner · matCam · orbit · staredown/intro`

**합마다 커버리지를 바꾼다** — 매 합이 같은 그림이면 카메라가 뻔해진다.
1합은 어깨너머(`dialogue`), 2합은 전경 없는 타이트 싱글(`dlgTight`), 3합 결정타는
정면에서 천천히 미는 슬로 푸시(`dlgPush`). 결정타 직전엔 눈높이 반원 아크(`arc`)로 숨을 고르고,
큰 펀치가 꽂히면 화각을 44°에서 29°로 확 조이는 크래시 줌(`impact`)이 들어간다.

**대사가 카메라를 지휘한다.** 기본 컷은 `dialogue` — 듣는 쪽 어깨 너머로 말하는 쪽을 잡고
**발언이 끝날 때까지 안 끊는다**(컷 길이 = 대사 읽는 시간). 화자가 바뀌면 반대편으로 넘긴다(리버스 숏).
`lineSide`를 경기 내내 고정해 **180도 선을 넘지 않으므로** 레드는 늘 화면 한쪽에 선다.

원칙:

1. **말하는 동안은 안 끊는다.** 컷은 화자가 바뀔 때와 주먹이 들어갈 때만.
2. **컷 안에서 계속 움직인다.** 달리·푸시인·오빗. 고정 카메라는 CG처럼 보인다.
3. **사건 크기에 반응한다.** 크리티컬은 더치, 피니시는 로우앵글, 다운은 매트캠 → 슬로모 오빗 리플레이.

더치·스카이캠은 특수 상황 전용으로 강등했고 손떨림은 1/3로 줄였다 — 그 흔들림이 곧 산만함이었다.

### 카메라가 등을 잡지 않게

`dir`은 상대→대상 방향이라 `focus + dir*d`는 **등 뒤**가 된다. 얼굴(표지)을 잡으려면
`-dir` 쪽, 즉 상대편 쪽에서 봐야 한다. 어깨너머 컷도 듣는 쪽 바로 뒤가 아니라 **옆으로 크게 비켜** 선다 —
가분수라 몸통이 커서 축 위에 서면 앞사람이 화면 한가운데를 덮는다.

### 케이지가 시야를 가리지 않게

옥타곤 그물을 그대로 두면 화면을 다 덮는다. **카메라와 링 사이에 낀 패널만** 골라서 지운다
(패널마다 재질을 따로 주고, 바깥 법선과 카메라 방향의 내적으로 판별). 반대편 그물은 남겨서
케이지 안이라는 걸 알린다 — 실제 중계도 앞쪽 철망은 초점이 나가 사라진다.

### 세로 화면 보정

three.js의 `fov`는 세로 화각이다. 샷 화각은 전부 16:9 가로 기준으로 잡혀 있어서, 폰 세로(비율 0.46)에
그대로 쓰면 가로 화각이 15°까지 좁아져 표지에 코를 박는다. `fitToAspect()`가 가로 폭을 유지하도록
세로 화각을 62°까지 넓히고, 모자란 만큼은 카메라를 뒤로 뺀다.

## 6. 명단 — 16권 전부 퍼블릭 도메인

플라이급(변신 74쪽)부터 헤비급(전쟁과 평화 1225쪽)까지. 쪽수·출간연도·저자는 실제 값이다.

손자병법 · 변신 · 이상한 나라의 앨리스 · 자기만의 방 · 군주론 · 햄릿 · 지킬 박사와 하이드 씨 ·
위대한 개츠비 · 프랑켄슈타인 · 드라큘라 · 오만과 편견 · 종의 기원 · 모비 딕 · 죄와 벌 ·
돈키호테 · 전쟁과 평화

**상성** 10종도 실제 계보에서 뽑았다 — 울프가 셰익스피어에게 그의 누이를 묻고(자기만의 방→햄릿),
진화가 창조에게 누가 너를 만들었냐 묻고(종의 기원→프랑켄슈타인), 손자병법과 전쟁과 평화가
전쟁을 두고 붙는다.

## 7. 소리

오디오 파일 없이 WebAudio로 합성한다(정적 호스팅 유지). 개시종 · 타격(저역 쿵 + 노이즈 탁) ·
낱장 소리 · 관중 웅성거림(브라운 노이즈 로우패스, 흥분도에 따라 함성으로 부푼다).

## 8. 콘솔에서 밸런스 확인하기

```js
BOOKFIGHT.seedSweep(BOOKFIGHT.BOOKS[12], BOOKFIGHT.BOOKS[0], 60)  // 시드 60개 승률
BOOKFIGHT.simulate(a, b, '12345').events                          // 타임라인 원본
```
